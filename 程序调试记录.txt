
1、flash 读写测试成功，单字节进行读写操作。注意flash的地址选择，地址设置在文件task.h中。
0x01000~0x01004	存放0x55 +2bytes MyID+ My_channel + node_configured_flag
0x01010~0x01012 存放0x55+0g读取AD值
0x01013~0x01015 存放0x55+500g读取AD值

2、使用LPM3的低功耗类型，TimerA最大唤醒周期16s，而使用Watchdog的最大唤醒时间1s，	
这种低功耗状态下，需要使用ACLK，也就是外部需要32768hz低频率晶振。



需要进行的内容：
1、扫频并绑定功能；
2、低功耗功能；(32768HZ的晶振）
3、无线的重发机制；